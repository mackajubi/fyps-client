import { ArchivedProjectsPipe } from './archived-projects.pipe';

describe('ArchivedProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArchivedProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
